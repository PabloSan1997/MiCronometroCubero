Table Usuario{
  id_user uuid [primary key]
  username varchar(100)
  nickname varchar(100)
  password varchar(100)
  idRes uuid 
}

Table Resoluciones{
  id_res uuid [primary key]
  algoritmo varchar(60)
  tiempo interger
  fecha Date
}

Ref: Usuario.idRes > Resoluciones.id_res